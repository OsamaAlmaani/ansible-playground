- name: create a file test
  hosts: "{{ hosts }}"
  become: no
  tasks:
    - name: create the file
      shell: |
        cd /home/gis/www/gce-maintenance-new/
        ./build.sh

        expect "Enter passphrase for key '/home/gis/.ssh/id_rsa':"
        send "{{ github_node_rsa_password }}\n

        expect "Enter passphrase for key '/home/gis/.ssh/id_rsa':"
        send "{{ github_node_rsa_password }}\n
