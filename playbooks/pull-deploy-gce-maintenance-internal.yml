- name: create a file test
  hosts: "servers"
  become: no
  tasks:
    - name: run deployment script
      command:
        cmd: echo "xyz"
        chdir: ~/www/gce-maintenance-new
      register: results
    - debug:
        var: results.stdout

